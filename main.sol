// SPDX-License-Identifier: GPL-3.0
pragma solidity ^0.7.4;

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

vv

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

var
contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}


contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}

contract Inbox {
    string public message;

    function inboxMessage(string memory initMessage) public {
        message = initMessage;
    }

    function setMessage(string memory newMessage) public {
        message = newMessage;
    }

    function getMessage() public view returns (string memory) {
        return message;
    }
}
